services:
  - type: web
    name: photostock-exif-service
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get install -y exiftool
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
